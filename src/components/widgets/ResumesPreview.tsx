import React from 'react';

const ExpertsPreview = () => {
    return (
        <div>
            {/*<div className="expert-preview">*/}
            {/*    <h2>{props.name}</h2>*/}
            {/*    <img src={props.profilePicture} alt={`Photo of ${props.name}`} />*/}
            {/*    <p>{props.bio}</p>*/}
            {/*    <h3>Skills:</h3>*/}
            {/*    <ul>*/}
            {/*        {props.skills.map(skill => (*/}
            {/*            <li key={skill}>{skill}</li>*/}
            {/*        ))}*/}
            {/*    </ul>*/}
            {/*</div>*/}
        </div>
    );
};

export default ExpertsPreview;